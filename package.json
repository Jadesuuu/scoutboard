{
  "name": "scoutboard",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "test": "pnpm --filter scoutboard-backend test"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
